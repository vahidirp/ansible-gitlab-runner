---
all:
  children:
    gitlab_runners:
      hosts:
        runner1:
          ansible_host: 10.10.34.36
          ansible_ssh_private_key_file: ~/.ssh/ssh-key
          ansible_port: 22
          gitlab_url: "https://gitlab.name.tld"
          gitlab_runner_token: "gl"
          gitlab_runner_tags: "docker,linux"
          gitlab_runner_network: "runner-net"
          runner_concurrent: "5"
          runner_interval: "2"
          runner_shutdown_timeout: "10"
          metrics_username: "admin"
          metrics_password: "promprom"
          traefik_subdomain: "traefik"
          domain: "example.runner.gitlab.name.tld"
          metrics_subdomain: "metrics.runner.gitlab.com"
          traefik_metrics_subdomain: "trmetrics.runner.gitlab.com"
          basic_auth_users:
            admin: "bcrypt2 or something"


          # gitlab_runner_extra_labels: "environment=staging, team=backend"

        # runner2:
        #   ansible_host: 192.168.1.101
        #   gitlab_url: "https://gitlab.another.com/"
        #   gitlab_runner_token: "TOKEN_2"
        #   gitlab_runner_tags: "kubernetes,ci"
        #   gitlab_runner_network: "runner-net"
        #  gitlab_runner_extra_labels: "environment=staging, team=backend"
